

## Replace Favicon with "JK" Logo

I'll create an SVG favicon with "JK" text and update `index.html` to use it.

### Changes

1. **Create `public/favicon.svg`** — An SVG with "JK" text on a dark gradient background, matching the portfolio's color scheme.

2. **Update `index.html`** — Replace the existing favicon reference with the new SVG:
   ```html
   <link rel="icon" href="/favicon.svg" type="image/svg+xml">
   ```

